/**
 * Add the provided redeemed reward to the store.
 * @param state
 * @param redeemedReward
 */
export function addRedeemedReward(state, redeemedReward) {
  state.redeemedReward = redeemedReward
}
