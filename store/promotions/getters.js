/**
 * Getter for returning promotions from store
 * @param state - The state of the  store.
 * @returns promotions from the state.
 */
export function getPromotions(state) {
  return state.promotions
}